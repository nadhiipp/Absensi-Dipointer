// Root-level build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Kotlin plugin
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.24"
        // Flutter Gradle plugin TIDAK perlu di sini
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
